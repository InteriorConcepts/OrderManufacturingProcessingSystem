<Application x:Class="OMPS.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:OMPS"
             xmlns:conv="clr-namespace:OMPS.Converters"
             xmlns:helpers="clr-namespace:OMPS"
             xmlns:viewModel="clr-namespace:OMPS.ViewModels"
             xmlns:pages="clr-namespace:OMPS.Pages"
             xmlns:windows="clr-namespace:OMPS.Windows"
             StartupUri="Windows/MainWindow.xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             ThemeMode="Dark">
            
             <!---->
    <Application.Resources>
        <ResourceDictionary>
            <!--
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/ClassicControlStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            -->


            <ResourceDictionary.MergedDictionaries>

                <!--
                <ResourceDictionary Source="Resources/DataGrid_Main.xaml" />
                <ResourceDictionary Source="Resources/DataGrid_SidePnl.xaml" />
                <ResourceDictionary Source="Resources/ClassicDarkStyles.xaml" />
                <ResourceDictionary Source="Resources/Landing_DataGrids.xaml" />
                -->

                <materialDesign:CustomColorTheme BaseTheme="Dark" PrimaryColor="DodgerBlue" SecondaryColor="AntiqueWhite"/>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />

                <!--<ResourceDictionary Source="pack://application:,,,/PresentationFramework.Fluent;component/Themes/Fluent.xaml" />-->

            </ResourceDictionary.MergedDictionaries>
            
            <BitmapImage x:Key="DefaultButtonImage" UriSource="pack://application:,,,/OMPS;component/Images/Save.png"/>
            <BitmapImage x:Key="MouseOverButtonImage" UriSource="pack://application:,,,/OMPS;component/Images/Save-hover.png"/>
            <BitmapImage x:Key="MouseDownButtonImage" UriSource="pack://application:,,,/OMPS;component/Images/Save-down.png"/>

            <!--
            <conv:NullToBooleanConverter x:Key="NullToBooleanConverter1" x:Name="NullToBooleanConverter1" />
            <conv:ScalingConverter x:Key="ScalingConverter1" x:Name="ScalingConverter1"/>
            <conv:BoolToVisibilityConverter x:Key="BoolToVisConverter1" x:Name="BoolToVisConverter1"/>
            <conv:StringToBoolConverter x:Key="StringToBoolConverter1" x:Name="StringToBoolConverter1" FalseValue="False" TrueValue="True"/>
            -->

            <Style x:Key="SaveBtn" TargetType="Button">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <Image x:Name="ButtonImage" Source="{StaticResource DefaultButtonImage}"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonImage" Property="Source" Value="{StaticResource MouseOverButtonImage}"/>
                                </Trigger>
                                <Trigger Property="IsMouseCaptured" Value="True">
                                    <Setter TargetName="ButtonImage" Property="Source" Value="{StaticResource MouseDownButtonImage}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>







            <Style x:Key="BtnLookupStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Height" Value="{Binding MainViewModel.FontSize_H2, FallbackValue=18}"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Margin" Value="0 4"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Background" Value="Transparent"/>
            </Style>

            <Style x:Key="BtnLookupImageStyle" TargetType="Image">
                <Setter Property="Width" Value="{Binding MainViewModel.FontSize_H2_1, FallbackValue=16}"/>
                <Setter Property="Height" Value="{Binding MainViewModel.FontSize_H2, FallbackValue=18}"/>
                <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality"/>
                <Setter Property="RenderOptions.EdgeMode" Value="Aliased"/>
                <Setter Property="Source" Value="pack://application:,,,/OMPS;component/Images/Binoculars.png"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="VerticalAlignment" Value="Top"/>
            </Style>

            <Style x:Key="InputLabel" TargetType="Label" BasedOn="{StaticResource MaterialDesignLabel}">
                <Setter Property="Height" Value="{Binding MainViewModel.FontSize_H2, FallbackValue=18}"/>
                <Setter Property="Padding" Value="0 1 2 1"/>
                <Setter Property="Margin" Value="0 4"/>
                <Setter Property="HorizontalContentAlignment" Value="Right"/>
                <Setter Property="FontSize" Value="{Binding MainViewModel.FontSize_H4}"/>
            </Style>

            <Style x:Key="InputComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignOutlinedComboBox}">
                <Setter Property="materialDesign:TextFieldAssist.TextFieldCornerRadius" Value="1"/>
                <Setter Property="materialDesign:TextFieldAssist.OutlinedBorderActiveThickness" Value="0.5"/>
                <Setter Property="BorderThickness" Value="0.5"/>
                <Setter Property="Padding" Value="2 1"/>
                <Setter Property="Margin" Value="0 4"/>
                <Setter Property="Height" Value="{Binding MainViewModel.FontSize_H2, FallbackValue=18}"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="FontSize" Value="{Binding MainViewModel.FontSize_H4}"/>
            </Style>

            <Style x:Key="InputStyle" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
                <Setter Property="Height" Value="{Binding MainViewModel.FontSize_H2, FallbackValue=18}"/>
                <Setter Property="Margin" Value="0 4"/>
                <Setter Property="materialDesign:TextFieldAssist.TextFieldCornerRadius" Value="1"/>
                <Setter Property="materialDesign:TextFieldAssist.OutlinedBorderActiveThickness" Value="0.5"/>
                <Setter Property="BorderThickness" Value="0.5"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="Padding" Value="0 1"/>
                <Setter Property="TextWrapping" Value="NoWrap"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="MinWidth" Value="70"/>
                <Setter Property="FontSize" Value="{Binding MainViewModel.FontSize_H4}"/>
            </Style>

            <Style x:Key="InputLookupStyle" TargetType="TextBox" BasedOn="{StaticResource InputStyle}">
                <Setter Property="materialDesign:TextFieldAssist.HasLeadingIcon" Value="True"/>
                <Setter Property="materialDesign:TextFieldAssist.LeadingIconSize" Value="{Binding MainViewModel.FontSize_H4, FallbackValue=12}"/>
                <Setter Property="materialDesign:TextFieldAssist.LeadingIcon" Value="LinkVariant"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
