<Window x:Name="MainWin" x:Class="OMPS.Windows.MainWindow" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OMPS"
        xmlns:Components="clr-namespace:OMPS.Components"
        xmlns:pages="clr-namespace:OMPS.Pages"
        xmlns:backdropblur="clr-namespace:BlurredBackground.WPF;assembly=BlurredBackground.WPF"
        xmlns:viewModel="clr-namespace:OMPS.viewModel"
        xmlns:Ext="clr-namespace:OMPS" Width="1025" Height="600"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
        mc:Ignorable="d" UseLayoutRounding="False" Closing="MainWindow_Closing"
        Title="MainWindow" MinHeight="600" MinWidth="1025" Background="#111"
        Style="{StaticResource MaterialDesignWindow}" WindowStyle="None" ResizeMode="CanResizeWithGrip" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <local:ScalingConverter x:Key="ScalingConverter1"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisConverter1"/>
    </Window.Resources>
    <Window.CacheMode>
        <BitmapCache RenderAtScale="2" EnableClearType="True" SnapsToDevicePixels="True"/>
    </Window.CacheMode>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" NonClientFrameEdges="Bottom" ResizeBorderThickness="4"/>
    </WindowChrome.WindowChrome>

    <Window.TaskbarItemInfo>
        <TaskbarItemInfo>
            <TaskbarItemInfo.Overlay>
                <DrawingImage>/Images/Interior-Concepts-Logo.png</DrawingImage>
            </TaskbarItemInfo.Overlay>
        </TaskbarItemInfo>
    </Window.TaskbarItemInfo>
    <Window.FocusVisualStyle>
        <Style/>
    </Window.FocusVisualStyle>
    <!--
    <Window.DataContext>
        <viewModel:Main_ViewModel/>
    </Window.DataContext>
    -->
    <Grid x:Name="Grid_Main" Margin="2" DataContext="{Binding ElementName=MainWin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="{Binding DataContext.FontSize_H1, FallbackValue=40, Converter={StaticResource ScalingConverter1}, ConverterParameter=1.5}"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="{Binding DataContext.FontSize_H1, FallbackValue=24}"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <materialDesign:ColorZone Grid.Row="0" Grid.ColumnSpan="2" Padding="0" Foreground="#111">
            <Grid x:Name="grid_TopBar" DataContext="{Binding ElementName=MainWin}" Height="{Binding DataContext.FontSize_H1, FallbackValue=40, Converter={StaticResource ScalingConverter1}, ConverterParameter=1.5}" MouseDown="grid_TopBar_MouseDown" Background="FloralWhite" IsHitTestVisible="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="10"/>
                </Grid.RowDefinitions>
                <Rectangle Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="5" VerticalAlignment="Top" Height="2">
                    <Rectangle.Fill>
                        <ImageBrush ImageSource="pack://application:,,,/OMPS;component/Images/gradient.png" Stretch="Fill"/>
                    </Rectangle.Fill>
                </Rectangle>
                <StackPanel Grid.Row="0" Grid.Column="0" Grid.RowSpan="2"
                            Orientation="Horizontal" HorizontalAlignment="Stretch"
                            VerticalAlignment="Center">
                    <Button x:Name="Btn_ToggleSideNav"
                            HorizontalContentAlignment="Center"
                            VerticalAlignment="Center" HorizontalAlignment="Stretch"
                            Style="{StaticResource MaterialDesignFlatButton}" materialDesign:ButtonAssist.CornerRadius="1"
                            Margin="2 0 0 0" Padding="1" Foreground="#3d3333"
                            Click="Btn_ToggleSideNav_Click">
                        <materialDesign:PackIcon Kind="DockLeft" Width="{Binding DataContext.FontSize_H1_2, FallbackValue=24}" Height="{Binding DataContext.FontSize_H1_2, FallbackValue=24}"/>

                    </Button>

                    <Button x:Name="Btn_Home"
                            Height="{Binding DataContext.FontSize_H1, FallbackValue=24}"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Margin="2 0 0 0" Padding="1" Foreground="#3d3333"
                            VerticalAlignment="Center" HorizontalAlignment="Stretch"
                            Click="Btn_Home_Click">
                        <materialDesign:PackIcon Kind="Home" Width="{Binding DataContext.FontSize_H1_2, FallbackValue=24}" Height="{Binding DataContext.FontSize_H1_2, FallbackValue=24}"/>
                    </Button>
                    <Button x:Name="Btn_Back"
                            Height="{Binding DataContext.FontSize_H1, FallbackValue=24}"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Margin="2 0 0 0" Padding="1" Foreground="#3d3333"
                            IsEnabled="{Binding DataContext.CanPrevious, ElementName=MainWin, UpdateSourceTrigger=PropertyChanged}"
                            VerticalAlignment="Center" HorizontalAlignment="Stretch"
                            Click="Btn_Back_Click"
                            MouseEnter="Btn_Back_MouseEnter" MouseLeave="Btn_Back_MouseLeave">
                        <materialDesign:PackIcon Kind="SwapHorizontalBold" Width="{Binding DataContext.FontSize_H1_2, FallbackValue=24}" Height="{Binding DataContext.FontSize_H1_2, FallbackValue=24}"/>
                    </Button>
                    <Button x:Name="Btn_Settings"
                            Height="{Binding DataContext.FontSize_H1, FallbackValue=24}"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Margin="2 0 0 0" Padding="1" Foreground="#3d3333"
                            VerticalAlignment="Center" HorizontalAlignment="Stretch"
                            Click="Btn_Settings_Click">
                        <materialDesign:PackIcon Kind="Cog" Width="{Binding DataContext.FontSize_H1_2, FallbackValue=24}" Height="{Binding DataContext.FontSize_H1_2, FallbackValue=24}"/>
                    </Button>
                    <Button x:Name="Btn_Chat"
                            Height="{Binding DataContext.FontSize_H1, FallbackValue=24}"
                            IsEnabled="True"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Margin="2 0 0 0" Padding="1" Foreground="#3d3333"
                            VerticalAlignment="Center" HorizontalAlignment="Stretch"
                            Click="Btn_Chat_Click">
                        <materialDesign:PackIcon Kind="TerminalNetworkOutline" Width="{Binding DataContext.FontSize_H1_2, FallbackValue=24}" Height="{Binding DataContext.FontSize_H1_2, FallbackValue=24}"/>
                    </Button>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="5" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Rectangle Width="{Binding DataContext.FontSize_H1, FallbackValue=22}" Margin="0 4 0 2" Fill="#FF2D2E31">
                        <Rectangle.OpacityMask>
                            <ImageBrush ImageSource="pack://application:,,,/OMPS;component/Images/Interior-Concepts-Logo.png" Stretch="Uniform" TileMode="None"/>
                        </Rectangle.OpacityMask>
                        <Rectangle.Effect>
                            <DropShadowEffect BlurRadius="3" Color="#FF2D2E31" ShadowDepth="1" RenderingBias="Performance" Opacity="0.3"/>

                        </Rectangle.Effect>
                    </Rectangle>
                    <Label FontSize="{Binding DataContext.FontSize_H1, FallbackValue=24}" FontWeight="Black" Foreground="#3d3333"
                           VerticalAlignment="Stretch" VerticalContentAlignment="Stretch"
                           Padding="4 0" HorizontalAlignment="Center">
                        <Label.Content>
                            <StackPanel>
                                <TextBlock Text="Production Bridge"/>
                                <Rectangle Margin="0 -8 0 0" Height="2" Fill="#FF2D2E31" SnapsToDevicePixels="True"/>
                            </StackPanel>
                        </Label.Content>
                        <Label.Effect>
                            <DropShadowEffect BlurRadius="3" Color="#FF2D2E31" ShadowDepth="1" RenderingBias="Performance" Opacity="0.3"/>

                        </Label.Effect>
                    </Label>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="4" Grid.RowSpan="2" Margin="0" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="Btn_WinMin" Click="Btn_WinMin_Click" Style="{StaticResource MaterialDesignFlatButton}" Foreground="#3d3333" Margin="4 0 0 0" Padding="3 0"
                            Width="Auto"
                            VerticalAlignment="Center" HorizontalAlignment="Stretch">
                        <materialDesign:PackIcon Kind="MinimizE" Width="{Binding DataContext.FontSize_H1_2, FallbackValue=24}" Height="{Binding DataContext.FontSize_H1_2, FallbackValue=24}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                    <Button x:Name="Btn_WinMax" Click="Btn_WinMax_Click" Style="{StaticResource MaterialDesignFlatButton}" Foreground="#3d3333" Margin="4 0 0 0" Padding="3 0"
                            Width="Auto"
                            VerticalAlignment="Center" HorizontalAlignment="Stretch">
                        <materialDesign:PackIcon Kind="WindowMaximize" Width="{Binding DataContext.FontSize_H1_2, FallbackValue=24}" Height="{Binding DataContext.FontSize_H1_2, FallbackValue=24}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                    <Button x:Name="Btn_WinClose" Click="Btn_WinClose_Click" Style="{StaticResource MaterialDesignFlatButton}" Foreground="#9f3847" Margin="4 0 0 0" Padding="3 0"
                            Width="Auto" Height="Auto"
                            VerticalAlignment="Center" HorizontalAlignment="Stretch">
                        <materialDesign:PackIcon Kind="CloseBoxOutline" Width="{Binding DataContext.FontSize_H1_2, FallbackValue=24}" Height="{Binding DataContext.FontSize_H1_2, FallbackValue=24}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                </StackPanel>
                <TextBox x:Name="Txt_Url" Grid.Row="1" FontSize="{Binding DataContext.FontSize_H9, FallbackValue=8}" materialDesign:TextFieldAssist.TextFieldCornerRadius="1" Opacity="0.8" VerticalAlignment="Top" Foreground="#303843" Grid.Column="2" Text="pbridge://order-search" TextAlignment="Center" VerticalContentAlignment="Center" Padding="10 0" Margin="100 0">
                    <TextBox.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0" MappingMode="RelativeToBoundingBox" SpreadMethod="Pad">
                            <GradientStop Color="#aabcad9f " Offset="0"/>
                            <GradientStop Color="#aa87857c" Offset="1"/>
                        </LinearGradientBrush>
                    </TextBox.Background>
                </TextBox>
            </Grid>
        </materialDesign:ColorZone>
        <Rectangle Grid.Row="1" Grid.ColumnSpan="2" Height="2" Fill="#FF2D2E31" VerticalAlignment="Top"/>
        <!--
        <Grid Grid.Row="0">
            <materialDesign:PackIcon Height="Auto" VerticalAlignment="Stretch" VerticalContentAlignment="Center" Kind="HomeOutline"/>
        </Grid>
        -->
        <materialDesign:ColorZone Grid.Row="2" Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="#151515" Margin="0">
            <Grid x:Name="Grid_Contents" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" DataContext="{Binding DataContext}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" x:Name="Spnl_FrameTabs" Orientation="Horizontal" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="#111">
                    <RadioButton x:Name="TabBtn_Landing" Style="{DynamicResource MaterialDesignTabRadioButtonBottom}"
                                 Content="Landing" Tag="{x:Static local:PageTypes.Home}" Padding="6 0"
                                 Visibility="{Binding Home_IsEnabled, Mode=OneWay, Converter={StaticResource BoolToVisConverter1}, ConverterParameter=True}"
                                 Height="21" IsChecked="{Binding Home_IsSelected, Mode=OneWay}"
                                 Checked="RadioButton_Checked"
                                 FontSize="{Binding FontSize_H4, FallbackValue=12}"/>
                    <RadioButton x:Name="TabBtn_OrderSearch" Style="{DynamicResource MaterialDesignTabRadioButtonBottom}"
                                 Content="Order Search" Tag="{x:Static local:PageTypes.OrderSearch}" Padding="6 0"
                                 Visibility="{Binding OrderSearch_IsEnabled, Mode=OneWay, Converter={StaticResource BoolToVisConverter1}, ConverterParameter=True}"
                                 Height="21" IsChecked="{Binding OrderSearch_IsSelected, Mode=OneWay}"
                                 Checked="RadioButton_Checked"
                                 FontSize="{Binding FontSize_H4, FallbackValue=12}"/>
                    <RadioButton x:Name="TabBtn_LineItemInfo" Style="{DynamicResource MaterialDesignTabRadioButtonBottom}"
                                 Content="LineItem Info" Tag="{x:Static local:PageTypes.EngOrder}" Padding="6 0"
                                 Visibility="{Binding EngOrder_IsSelected, Mode=OneWay, Converter={StaticResource BoolToVisConverter1}, ConverterParameter=True}"
                                 Height="21" IsChecked="{Binding EngOrder_IsSelected, Mode=OneWay}"
                                 Checked="RadioButton_Checked"
                                 FontSize="{Binding FontSize_H4, FallbackValue=12}"/>
                    <RadioButton x:Name="TabBtn_QuoteOrder" Style="{DynamicResource MaterialDesignTabRadioButtonBottom}"
                                 Content="Quote Order" Tag="{x:Static local:PageTypes.QuoteOrder}" Padding="6 0"
                                 Visibility="{Binding QuoteOrder_IsEnabled, Mode=OneWay, Converter={StaticResource BoolToVisConverter1}, ConverterParameter=True}"
                                 Height="21" IsChecked="{Binding QuoteOrder_IsSelected, Mode=OneWay}"
                                 Checked="RadioButton_Checked"
                                 FontSize="{Binding FontSize_H4, FallbackValue=12}"/>
                </StackPanel>
                <ContentControl Grid.Row="1" x:Name="CC_Landing" Content="{Binding Home_VM, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Visibility="Collapsed"/>
                <ContentControl Grid.Row="1" x:Name="CC_Login" Content="{Binding Login_VM, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Visibility="Collapsed"/>
                <ContentControl Grid.Row="1" x:Name="CC_OrderSearch" Content="{Binding OrderSearch_VM, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Visibility="Collapsed"/>
                <ContentControl Grid.Row="1" x:Name="CC_EngOrder" Content="{Binding EngOrder_VM, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Visibility="Collapsed"/>
                <ContentControl Grid.Row="1" x:Name="CC_QuoteOrder" Content="{Binding QuoteOrder_VM, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Visibility="Collapsed"/>
            </Grid>
        </materialDesign:ColorZone>

        <DockPanel Grid.Row="2" Grid.Column="1" Margin="0 32 0 0" Width="Auto" MaxWidth="400" HorizontalAlignment="Left" Background="#111">
            <Border Width="Auto" BorderThickness="0 0 2 0" BorderBrush="#FF2D2E31">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" Color="#000" Direction="-45" Opacity="0.7" ShadowDepth="3" />
                </Border.Effect>
                <Border.Background>
                    <ImageBrush ImageSource="pack://application:,,,/OMPS;component/Images/fluent_web_dark_bg.png" Opacity="0.2" TileMode="None" Stretch="UniformToFill"/>
                </Border.Background>
                <StackPanel x:Name="Spnl_SideNav" Visibility="Collapsed" Width="Auto">
                    <Expander FontWeight="DemiBold" Foreground="#e9e9e9" Background="Transparent" HorizontalAlignment="Stretch">
                        <Expander.Header>
                            <StackPanel>
                                <TextBlock Text="Orders" Padding="0" Margin="0" FontSize="{Binding DataContext.FontSize_H3, FallbackValue=16}"/>
                                <Rectangle Height="2" Fill="#FF3B5492"/>
                            </StackPanel>
                        </Expander.Header>
                        <StackPanel HorizontalAlignment="Stretch" Background="#AA3B5492" TextBlock.Foreground="{DynamicResource MaterialDesignBody}">
                            <Button Foreground="#eee" VerticalAlignment="Center" FontWeight="Medium" Style="{StaticResource MaterialDesignFlatButton}" Padding="2" FontSize="{Binding DataContext.FontSize_H4, FallbackValue=12}" Tag="{x:Static local:PageTypes.OrderSearch}" Click="Btn_SideNav_Click">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Stretch">
                                        <TextBlock Text="Order Lines" VerticalAlignment="Center"/>
                                        <materialDesign:PackIcon Foreground="LightGray" Margin="4 0 0 0" VerticalAlignment="Center" Height="12" Width="12" Kind="ListBoxOutline"/>
                                    </StackPanel>
                                </Button.Content>
                            </Button>
                        </StackPanel>
                    </Expander>
                    <Expander FontWeight="DemiBold" Foreground="#e9e9e9" Background="Transparent" HorizontalAlignment="Stretch">
                        <Expander.Header>
                            <StackPanel>
                                <TextBlock Text="Marketing" Padding="0" Margin="0" FontSize="{Binding DataContext.FontSize_H3, FallbackValue=16}"/>
                                <Rectangle Height="2" Fill="#FF3B5492"/>
                            </StackPanel>
                        </Expander.Header>
                        <StackPanel HorizontalAlignment="Stretch" Background="#AA3B5492" TextBlock.Foreground="{DynamicResource MaterialDesignBody}">
                            <Button Foreground="#eee" FontWeight="Medium" Style="{StaticResource MaterialDesignFlatButton}" Padding="2" FontSize="{Binding DataContext.FontSize_H4, FallbackValue=12}" Tag="{x:Static local:PageTypes.None}" Click="Btn_SideNav_Click">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Stretch">
                                        <TextBlock Text="Product Catalog" VerticalAlignment="Center"/>
                                        <materialDesign:PackIcon Foreground="LightGray" Margin="4 0 0 0" VerticalAlignment="Center" Height="12" Width="12" Kind="LinkBoxVariantOutline"/>
                                    </StackPanel>
                                </Button.Content>
                            </Button>
                            <Button Foreground="#eee" FontWeight="Medium" Style="{StaticResource MaterialDesignFlatButton}" Padding="2" FontSize="{Binding DataContext.FontSize_H4, FallbackValue=12}" Tag="{x:Static local:PageTypes.None}" Click="Btn_SideNav_Click">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Stretch">
                                        <TextBlock Text="Manuf Lines" VerticalAlignment="Center"/>
                                        <materialDesign:PackIcon Foreground="LightGray" Margin="4 0 0 0" VerticalAlignment="Center" Height="12" Width="12" Kind="ToolboxOutline"/>
                                    </StackPanel>
                                </Button.Content>
                            </Button>
                        </StackPanel>
                    </Expander>
                </StackPanel>
            </Border>
        </DockPanel>

        <materialDesign:ColorZone Grid.Row="3" Grid.ColumnSpan="2" Mode="Dark" Background="#191818" Foreground="#CCC" Margin="0" VerticalAlignment="Center" VerticalContentAlignment="Center" Visibility="Visible">
            <Grid x:Name="statusbar" VerticalAlignment="Center" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Separator Grid.Column="0" Grid.ColumnSpan="2" VerticalAlignment="Top" Margin="0"/>
                <StackPanel Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Stretch" Orientation="Horizontal" Margin="5 0 0 0">
                    <Image x:Name="Img_NetworkStatus" Margin="3" SnapsToDevicePixels="True"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       RenderOptions.EdgeMode="Aliased" RenderSize="32,32"
                       Source="pack://application:,,,/OMPS;component/Images/Network.png"/>
                    <Label Content="Online" VerticalAlignment="Center" VerticalContentAlignment="Center"
                           Foreground="#BBB" FontSize="{Binding DataContext.FontSize_H4, FallbackValue=12}"
                           Padding="0,0,0,0">
                        <Label.ContextMenu>
                            <ContextMenu BorderThickness="1" Background="#99292929" Padding="2" Margin="0" materialDesign:MenuAssist.MenuItemsPresenterMargin="1">
                                <Separator>
                                    <Separator.Template>
                                        <ControlTemplate TargetType="Separator">
                                            <StackPanel Height="24">
                                                <TextBlock TextAlignment="Left" Padding="2" Text="Title!!!" FontSize="{Binding }" />
                                                <Separator/>
                                            </StackPanel>
                                        </ControlTemplate>
                                    </Separator.Template>
                                </Separator>
                                <MenuItem Header="test123" Padding="1"/>
                            </ContextMenu>
                        </Label.ContextMenu>
                    </Label>
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Stretch" Orientation="Horizontal" Margin="0 0 5 0">
                    <Label x:Name="Lbl_Date" Content="Monday, September 22, 2025" Foreground="#BBB" FontSize="{Binding FontSize_Base}" VerticalAlignment="Center" VerticalContentAlignment="Center" Panel.ZIndex="9999" HorizontalContentAlignment="Center" Margin="0,0,10,0" Padding="0,0,0,0"/>
                    <Label x:Name="Lbl_Time" Content="11:42:15" Foreground="#BBB" FontSize="{Binding FontSize_Base}" VerticalAlignment="Center" VerticalContentAlignment="Center" Panel.ZIndex="9999" HorizontalContentAlignment="Center" Padding="0,0,0,0"/>
                </StackPanel>
            </Grid>
        </materialDesign:ColorZone>
    </Grid>

</Window>
