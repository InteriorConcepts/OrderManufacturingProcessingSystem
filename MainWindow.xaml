<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OMPS"
        xmlns:Components="clr-namespace:OMPS.Components" xmlns:Pages="clr-namespace:OMPS.Pages" x:Name="MainWin" x:Class="OMPS.MainWindow"
        mc:Ignorable="d" UseLayoutRounding="False"
        Title="MainWindow" Height="450" Width="800" MinHeight="450" MinWidth="650"
        Background="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="24"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
        </Grid>
        <Frame Grid.Row="1" x:Name="MainFrame"/>
        <Grid x:Name="statusbar" Grid.Row="2" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
          Background="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Stretch" Orientation="Horizontal" Margin="5 0 0 0">
                    <Image x:Name="Img_NetworkStatus" Margin="3" SnapsToDevicePixels="True"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       RenderOptions.EdgeMode="Aliased" RenderSize="32,32"
                       Source="pack://application:,,,/OMPS;component/Images/Network.png"/>
                    <Label Content="Connected" Foreground="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                       VerticalAlignment="Center" VerticalContentAlignment="Center" FontSize="9pt" Padding="0,0,0,0"/>
                </StackPanel>
            <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Stretch" Orientation="Horizontal" Margin="0 0 5 0">
                <Label x:Name="Lbl_Date" Content="Monday, September 22, 2025" FontSize="9pt" Foreground="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" VerticalContentAlignment="Center" Panel.ZIndex="9999" HorizontalContentAlignment="Center" Margin="0,0,10,0" Padding="0,0,0,0"/>
                <Label x:Name="Lbl_Time" Content="11:42:15" FontSize="9pt" Foreground="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" VerticalContentAlignment="Center" Panel.ZIndex="9999" HorizontalContentAlignment="Center" Padding="0,0,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>

</Window>
